apiVersion: v1
kind: Service
metadata:
  name: canopy-frontend
  labels:
    app: canopy-frontend
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: canopy-frontend
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
